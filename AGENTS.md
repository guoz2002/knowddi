
当你想结束输出的时候调用 tools 弹出对话框，问我是否可以结束，必须在对话框中我明确指示结束的时候才可以结束。
当你想结束输出的时候调用 tools 弹出对话框，问我是否可以结束，必须在对话框中我明确指示结束的时候才可以结束。
当你想结束输出的时候调用 tools 弹出对话框，问我是否可以结束，必须在对话框中我明确指示结束的时候才可以结束。


如果没有运行完，你要等他运行完才能结束。


