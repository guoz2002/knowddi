"""
第六章：总结与展望
"""

CHAPTER_TITLE = "第六章  总结与展望"

SECTIONS = {
    "6.1": {
        "title": "工作总结",
        "content": """
本文以药物-药物相互作用（DDI）预测为核心研究问题，以KnowDDI模型为基础实现平台，围绕图结构学习机制解耦与面向边缘部署的轻量化重构两条主线，完成了以下研究工作：

（1）KnowDDI基准复现

在本地研究环境中搭建了基于Python 3.11、PyTorch 2.0和DGL 1.1的深度学习实验框架，解决了新硬件平台与原始代码环境不兼容的问题。成功完成了KnowDDI模型在DrugBank（Macro-F1 = 91.49、ACC = 92.86、Cohen's κ = 91.53）和TWOSIDES（AUROC = 95.44、AUPRC = 94.11）两个基准数据集上的复现，结果与原论文接近，建立了后续改进实验的可靠对比基线。

（2）图结构学习机制的消融解耦

设计并实施了系统性的受控消融实验，构建了baseline、仅去噪（Denoise-only）、仅补全（Completion-only）和去噪+补全（Denoise+Completion）四组受控变体。在DrugBank与TWOSIDES两个数据集上开展对比实验，首次对图结构学习中的去噪与补全机制进行了定量解耦分析。

主要发现：在DrugBank多分类任务上，去噪机制更有利于提升类别均衡意义下的预测性能（Macro-F1），补全机制更有利于增强整体预测一致性（ACC、Cohen's κ）；在TWOSIDES多标签任务上，原始基线模型表现最优，两种机制均未带来显著增益，且补全模块的激活比例极低（0.62%~0.79%）。上述结果表明，去噪与补全具有差异化的边际贡献模式和数据集相关的适用边界。

（3）面向边缘部署的轻量化重构与评测

提出了"拓扑-参数"协同压缩策略，设计并评估了仅子图稀疏采样（Sparse-only）、仅多维特征压缩（Feature-only）和协同优化（Sparse+Feature）三类轻量化方案。

    在DrugBank上，特征压缩方案（50%压缩比）在仅损失0.58% Macro-F1的前提下实现了约48%的显存节省和推理时长缩短，是精度损失最小的单维度轻量化策略；在TWOSIDES上，Sparse-only方案的AUROC（95.50）和AUPRC（94.24）小幅高于基线（结合标准差分析属于正常波动），三类轻量化方案对TWOSIDES的精度影响均极小（变化不超过0.21%），但效率收益也相对有限（推理时长节省不超过10%），说明该场景的计算瓶颈主要在数据规模层面。{{B:从工程实施层面再做一次回顾：本文的全部改动收敛在三个文件——pytorch/model/gsl_model.py（新增EdgeGateNetwork的三路输出与gsl_mode/use_denoise/use_completion字段）、pytorch/data_processor/subgraph_extraction.py（暴露max_nodes_per_hop与max_links两个稀疏阈值）、pytorch/train.py（新增对应的命令行入口并透传到模型构造），其余如GraphSAGE的pre_embed维度、分类器、训练循环均保持原状，这种"最小改动"路径使两条主线工作可以独立装载或卸载到任何基于KnowDDI的衍生模型上，便于后续工作复用。}}

[YELLOW]本文全部改动收敛于三个核心文件的调用关系及数据流向如图6-1所示，train.py命令行参数沿两条链路分别注入subgraph_extraction.py（拓扑稀疏化）和EdgeGateNetwork（参数压缩），两路汇入分类器后输出指标，改动边界清晰，便于后续工作在此基础上独立拓展。

[BLUEFIG]【待插图：图6-1 本文改动收敛于三个核心文件的依赖关系图（EdgeGateNetwork ↔ subgraph_extraction ↔ train.py 命令行入口的数据流向）】
"""
    },
    "6.2": {
        "title": "研究局限性",
        "content": """
本研究存在以下几点主要局限性：

（1）实验规模限制：本文的消融实验仅在DrugBank和TWOSIDES两个数据集上开展，研究结论的普适性有待在更多DDI数据集（如OGB-biokg、OGBL-ddi等）上进一步验证。

（2）轻量化策略的局限性：本文采用的子图稀疏采样策略基于随机采样或简单阈值控制，未引入基于节点重要性（如PageRank、度中心性）的自适应采样策略。更智能的稀疏化方法可能在相同压缩比例下取得更小的精度损失。

（3）硬件评测环境的代表性：本文的推理时延和显存峰值测量在标准GPU服务器环境中进行，未在真实的医疗边缘设备（如ARM Cortex处理器、嵌入式GPU）上进行实际部署测试。边缘端的实际推理性能可能与本文报告结果存在差异。

（4）可解释性分析的深度：本文主要通过定量消融实验分析图结构学习机制，未深入展开对典型药物对的案例研究和生物医学路径解析，可解释性分析的深度有待加强。

（5）补全机制的设计局限：当前补全机制在TWOSIDES上的激活比例极低（0.62%~0.79%），说明补全策略对该数据集的适应性不足，未来需要针对不同数据集特性设计更具自适应性的补全阈值策略。
"""
    },
    "6.3": {
        "title": "研究展望",
        "content": """
基于本文的研究成果和局限性分析，对未来研究方向提出以下展望：

（1）自适应图结构学习策略

未来可以在当前基于固定阈值的图稀疏化基础上，引入基于图拓扑统计（如节点度分布、聚类系数）的自适应阈值机制，使去噪与补全操作能够根据不同药物对局部子图的特征动态调整激活强度，提升图结构学习机制在不同数据分布下的普适性。

（2）面向真实边缘设备的模型量化与部署

在本文轻量化架构的基础上，结合模型量化（INT8/FP16混合精度）、算子融合等推理引擎优化技术，将轻量化KnowDDI模型实际部署到ARM Cortex-M、NVIDIA Jetson等医疗边缘硬件平台上，测量真实设备上的推理延迟和功耗，为面向医疗IoT场景的DDI实时预警系统提供实践基础。

（3）与大语言模型（LLM）的融合

近年来，以GPT-4、Med-PaLM为代表的大型预训练语言模型在生物医学领域展现出强大的知识理解和推理能力。将LLM的语义理解能力与本文的图结构学习框架结合，有望构建具有更强背景知识整合能力的DDI预测系统，进一步提升小样本场景下的泛化性能。

（4）多模态药物表示学习

当前KnowDDI主要依赖知识图谱的拓扑结构信息作为药物表示。未来可引入药物分子指纹、SMILES字符串、蛋白质序列等多模态特征，构建更丰富的药物表示，结合图结构学习的关系建模能力，有望进一步提升DDI预测的精度和可解释性。

（5）跨数据集迁移学习

消融实验揭示了图结构学习机制在DrugBank和TWOSIDES上的适用边界差异。未来可探索基于元学习（Meta-Learning）或领域自适应（Domain Adaptation）的跨数据集迁移策略，使模型能够自适应地在不同DDI数据集特性下调整图结构学习的策略，提升模型的跨场景泛化能力。
"""
    }
}
