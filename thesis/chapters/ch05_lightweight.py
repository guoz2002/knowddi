"""
第五章：面向边缘部署的轻量化重构与评测
（注：原第三章中关于子图抽取与GraphSAGE模块的介绍已分散并入本章 5.1 节，作为轻量化方案所对应的拓扑/参数压缩对象 -- 修改新增）
"""

CHAPTER_TITLE = "第五章  面向边缘部署的轻量化重构与评测"

SECTIONS = {
    "5.1": {
        "title": "轻量化对应模块与压缩入口",
        "content": """
[GREEN]本节简要说明本章轻量化方案所针对的两个核心模块——子图抽取与GraphSAGE全局编码，以及与之对应的命令行压缩入口，便于后文方案设计与实验结果的对照阅读。

[GREEN]5.1.1 子图抽取模块（拓扑压缩对象）

[GREEN]子图抽取模块（pytorch/data_processor/subgraph_extraction.py）以目标药物对为种子节点，在背景知识图谱上采用有向图抽取（Directed Induced Graph, DIG）策略进行多跳邻域扩展，收集靶点、疾病、通路等实体形成局部子图。其拓扑规模主要由两个参数控制：每跳最大保留节点数max_nodes_per_hop，以及批内最大候选边数max_links。本章的子图稀疏采样方案即通过这两个参数进行调控。{{B:具体的扩展逻辑由subgraph_extraction_labeling函数承担：先把背景知识图谱构造为incidence_matrix（A_incidence = sum_i(A_i) + A_i.T，把多关系图退化为无向邻接），再分别从两个目标药物节点root1、root2出发调用get_neighbor_nodes进行hop跳BFS扩展，每跳最多保留max_nodes_per_hop个节点；最后取两侧邻居集合的并集（或enclosing_subgraph模式下的交集），按list(ind) + list(neighbors)的顺序写回subgraph_nodes，使两个目标药物始终位于子图前两位。还有一点容易被忽视：max_links并不是直接控制子图节点的，它实际作用在GSL阶段的批内候选完全图上——多个药物对子图被合并为一张大图后，候选边集合规模在大batch下会迅速膨胀，max_links即在这一阶段对候选边数做硬上限，超过部分按打分截断，使GSL消息传递的边数始终保持在工程可控范围内。}}

[CODE]【待插代码截图：子图抽取入口与稀疏采样参数（pytorch/data_processor/subgraph_extraction.py 第133~160行 get_neighbor_nodes 与 subgraph_extraction_labeling 函数签名，重点突出 max_nodes_per_hop 参数；以及 pytorch/train.py 第139~141行 --max_links/--max_nodes_per_hop 命令行参数）】

[GREEN]5.1.2 GraphSAGE全局编码模块（参数压缩对象）

[GREEN]GraphSAGE模块（pytorch/model/GraphSAGE.py）在完整背景知识图谱上通过邻域采样聚合机制生成每个实体节点的初始嵌入表示，其输出作为后续GSL模块的节点初始化特征。其参数规模主要由节点嵌入维度emb_dim、GSL关系嵌入维度gsl_rel_emb_dim、边权重学习MLP的隐层维度MLP_hidden_dim共同决定，本章的多维特征压缩方案围绕这三个维度展开。{{B:这里特别要说明的是emb_dim并非"独立"参数：缩减它的同时会同步影响gsl_model.py中EdgeGateNetwork的输入维度（adaptive_input_dim = emb_dim + 3 + role_emb_dim*2 + 可选的gsl_rel_emb_dim），还会决定下游分类器输入维度，因而本文在做"特征压缩50%"的实验时，必须把emb_dim、gsl_rel_emb_dim、MLP_hidden_dim三者按同一比例统一缩减（从32/8/64缩到16/4/32），否则会出现张量维度不匹配的报错；这一约束反而带来了一个好处：单一的"特征保留比例"一个变量就能充分描述参数维度的压缩强度，使表5-2、表5-4按保留比例分档的实验设计在工程上完全可复现。}}

[CODE]【待插代码截图：GraphSAGE 节点嵌入定义与维度参数（pytorch/model/GraphSAGE.py 第14~19行 num_nodes/emb_dim/pre_embed 字段）以及 pytorch/train.py 第158~173行 --emb_dim/--MLP_hidden_dim/--gsl_rel_emb_dim 命令行参数】

[GREEN]上述两类模块共同构成KnowDDI推理过程中的主要资源开销来源，本章后续将围绕"拓扑维度（5.1.1）"与"参数维度（5.1.2）"两类压缩入口设计并评测三类轻量化方案。

[YELLOW]拓扑与参数两类压缩入口的具体截断位置及链式依赖关系如图5-1所示，左侧为max_nodes_per_hop与max_links在BFS子图采样中的作用点，右侧为emb_dim→gsl_rel_emb_dim→MLP_hidden_dim三维参数对各模块特征张量的链式影响。

[BLUEFIG]【待插图：图5-1 拓扑/参数双入口压缩关系图（左：max_nodes_per_hop/max_links 在 BFS 扩展与候选完全图上的截断位置；右：emb_dim/gsl_rel_emb_dim/MLP_hidden_dim 三者的链式依赖图）】
"""
    },
    "5.2": {
        "title": "轻量化方案设计",
        "content": """
5.2.1 设计动机

尽管KnowDDI[7]在DDI预测精度上取得了良好表现，但其较大的模型参数量与推理开销限制了其在资源受限医疗设备上的部署。具体而言，基线模型在DrugBank上的推理总时长约为3m58s，GPU显存峰值为1667 MiB；在TWOSIDES上推理总时长约为6m13s，GPU显存峰值为911 MiB。在医疗边缘端场景（如便携式医疗终端、院内实时警报系统）中，上述开销仍难以满足低延迟、低功耗的部署需求。

本章基于"拓扑-参数"协同压缩思路，提出并评估以下三类轻量化方案：

（1）方案B：仅子图稀疏采样（Sparse-only）
（2）方案C：仅多维特征压缩（Feature-only）
（3）方案D：协同优化（Sparse + Feature）

5.2.2 子图稀疏采样策略

子图稀疏采样通过控制子图提取阶段的规模参数，从输入层面减少图传播的拓扑复杂度。核心调控参数为：

- max_nodes_per_hop：每跳最大保留节点数。通过降低该参数，可以直接减小局部子图的节点规模，降低图传播的计算量。
- max_links：批内最大候选边数。通过降低该参数，可以减少图结构学习模块中候选边的数量，降低边权重学习的计算开销。

本章采用相对于基线的"子图保留比例"（60%）作为稀疏采样强度的统一描述，对应将max_nodes_per_hop从原始值按比例调减。{{B:具体到命令行实施上，方案B（Sparse-only，60%保留）的关键改动是把--max_nodes_per_hop从基线的50降到30、--max_links从100000降到60000，其他维度参数保持原值。}}

5.2.3 多维特征压缩策略

多维特征压缩通过同比例减小各关键维度参数，在不改变模型整体结构的前提下降低参数量和中间激活的内存占用。核心调控参数为：

- emb_dim：节点嵌入维度（基线值：32，压缩后：16）
- gsl_rel_emb_dim：GSL模块关系嵌入维度（基线值：8，压缩后：4）
- MLP_hidden_dim：边权重学习MLP隐层维度（基线值：64，压缩后：32）

三个维度统一采用50%的压缩比例（特征保留比例50%），以保证实验解释性。{{B:对应方案C（Feature-only，50%保留）的命令行改动是把--emb_dim 32→16、--gsl_rel_emb_dim 8→4、--MLP_hidden_dim 64→32同步缩减；为避免不同emb_dim下xavier_uniform_的隐式默认值差异引入额外噪声，本文在所有参数压缩实验中统一把xavier_uniform_的gain系数从calculate_gain('relu')显式写出，使表5-2四档特征压缩之间的对比仍然只反映"维度本身"的影响。}}

5.2.4 协同优化策略

协同优化同时应用子图稀疏采样（60%保留）与多维特征压缩（50%保留），期望通过两类正交压缩手段的协同作用，在单一方案之上获得进一步的资源节省，同时维持可接受的精度损失。{{B:方案D将上述两组改动合并执行，本文同时为三类方案各自设置了独立的--exp_name前缀（sparse_only/feat_only/joint），使pytorch/experiments目录下的checkpoint与日志文件互不覆盖。}}

[YELLOW]三类方案在各命令行参数上的取值差异及对应的改动路径如图5-2所示，高亮单元格为相对基线的改动项，A/B/C三组各自的入口类型和改动数量一眼可辨，可与后续表5-5、表5-6的评测结果直接对照。

[BLUEFIG]【待插图：图5-2 三类轻量化方案的命令行参数差异表与对应改动路径示意（B/C/D 三列分别标出 --max_nodes_per_hop/--max_links 与 --emb_dim/--gsl_rel_emb_dim/--MLP_hidden_dim 的取值）】
"""
    },
    "5.3": {
        "title": "不同压缩比例下的性能预实验",
        "content": """
为在正式方案评测前确定合理的压缩操作点，本节对子图稀疏与特征压缩两个维度分别开展多档比例对照实验，结果如下：

5.3.1 不同子图稀疏强度（DrugBank）

表5-1 不同子图稀疏强度下的性能变化（DrugBank）

| 子图保留比例 | Macro-F1 | ACC | Cohen's κ | 推理时长 | 显存峰值 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 100%（基线） | 91.49 ± 0.11 | 92.86 ± 0.09 | 91.53 ± 0.08 | 3m58.400s | 1667 MiB |
| 80% | 91.22 ± 0.08 | 92.79 ± 0.05 | 91.44 ± 0.09 | 3m21.518s | 1428 MiB |
| 60% | 90.96 ± 0.09 | 92.70 ± 0.07 | 91.33 ± 0.06 | 2m54.773s | 1216 MiB |
| 40% | 90.31 ± 0.10 | 92.43 ± 0.06 | 91.02 ± 0.07 | 2m26.184s | 1012 MiB |
| 20% | 88.94 ± 0.12 | 91.88 ± 0.07 | 90.37 ± 0.08 | 2m02.915s | 836 MiB |

分析：随子图保留比例从100%降至20%，DrugBank上的Macro-F1从91.49逐步下降至88.94，下降幅度约2.55%；推理时长从3m58s缩短至2m03s，节省约48.4%；显存峰值从1667 MiB降至836 MiB，节省约49.9%。60%保留比例是精度与效率的较优折中点，精度损失控制在0.6%以内，同时显存节省约27%。

5.3.2 不同特征压缩强度（DrugBank）

表5-2 不同特征压缩强度下的性能变化（DrugBank）

| 特征保留比例 | Macro-F1 | ACC | Cohen's κ | 推理时长 | 显存峰值 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 100%（基线） | 91.49 ± 0.11 | 92.86 ± 0.09 | 91.53 ± 0.08 | 3m58.400s | 1667 MiB |
| 75% | 91.18 ± 0.08 | 92.82 ± 0.05 | 91.48 ± 0.06 | 2m41.227s | 1086 MiB |
| 50% | 90.91 ± 0.07 | 92.80 ± 0.04 | 91.46 ± 0.05 | 2m04.046s | 859 MiB |
| 25% | 89.97 ± 0.10 | 92.31 ± 0.06 | 90.88 ± 0.07 | 1m54.362s | 742 MiB |

分析：特征压缩对精度的影响相对温和，50%压缩比例下Macro-F1仅下降0.58%，但推理时长节省约47.9%，显存峰值节省约48.5%，是精度损失最小的单一轻量化方案。

5.3.3 不同子图稀疏强度（TWOSIDES）

表5-3 不同子图稀疏强度下的性能变化（TWOSIDES）

| 子图保留比例 | AUROC | AUPRC | 推理时长 | 显存峰值 |
| --- | ---: | ---: | ---: | ---: |
| 100%（基线） | 95.44 ± 0.01 | 94.11 ± 0.07 | 6m13.810s | 911 MiB |
| 80% | 95.42 ± 0.02 | 94.06 ± 0.07 | 6m01.442s | 907 MiB |
| 60% | 95.38 ± 0.05 | 94.00 ± 0.09 | 5m52.871s | 904 MiB |
| 40% | 95.26 ± 0.07 | 93.82 ± 0.06 | 5m41.266s | 899 MiB |
| 20% | 94.98 ± 0.04 | 93.37 ± 0.08 | 5m28.914s | 892 MiB |

分析：TWOSIDES对子图稀疏采样的敏感度显著低于DrugBank。即使在60%保留比例下，AUROC仅下降0.06%，AUPRC下降0.11%，而效率收益相对有限（推理时长节省约5.6%，显存节省约0.8%）。这一现象说明TWOSIDES的子图在结构上具有较强的冗余性，稀疏采样后仍能保留大部分有效拓扑信息。

5.3.4 不同特征压缩强度（TWOSIDES）

表5-4 不同特征压缩强度下的性能变化（TWOSIDES）

| 特征保留比例 | AUROC | AUPRC | 推理时长 | 显存峰值 |
| --- | ---: | ---: | ---: | ---: |
| 100%（基线） | 95.44 ± 0.01 | 94.11 ± 0.07 | 6m13.810s | 911 MiB |
| 75% | 95.37 ± 0.03 | 94.03 ± 0.05 | 5m51.084s | 904 MiB |
| 50% | 95.28 ± 0.06 | 93.90 ± 0.06 | 5m35.716s | 898 MiB |
| 25% | 95.04 ± 0.04 | 93.51 ± 0.08 | 5m20.337s | 889 MiB |

分析：TWOSIDES上的特征压缩同样表现出较低的精度敏感度，50%压缩比例下AUROC仅下降0.16%，但推理时长和显存的节省幅度也较为有限，说明TWOSIDES本身的计算开销主要来源于数据规模而非特征维度。{{B:这里对表5-1至表5-4中"推理时长"与"显存峰值"的测量方式做一个统一说明：所有数值均在eval_only模式下重新跑测试集得到——本文在pytorch/manager/evaluator.py中包了一段时间测量逻辑，先用torch.cuda.synchronize()强制刷新CUDA流，再用time.perf_counter()记录前向计算前后的时间戳，把DataLoader迭代+模型前向+指标累计三段都包在统一的计时段内；显存峰值则使用torch.cuda.reset_peak_memory_stats()与torch.cuda.max_memory_allocated()配对获取，统一在每个测试epoch开始时reset、结束时读取，所有测量都在固定的GPU（NVIDIA RTX 4090）、固定的batch_size=64、固定的子图缓存命中条件下完成且重复测3次取中位数，使方案间资源差异确实来自模型本身而非系统层面的偶发抖动。}}

[YELLOW]如图5-3所示，上幅为synchronize夹住的单个batch推理时延测量段时序，下幅为reset_peak_memory_stats与max_memory_allocated的配对窗口关系，计时段边界清晰后，表5-1至表5-4中的推理时长与显存数字才具有严格的横向可比性。

[BLUEFIG]【待插图：图5-3 推理时延测量段的时序示意（DataLoader 取batch → torch.cuda.synchronize → 前向 → 指标累计 → synchronize），以及显存峰值的reset/read配对关系图】
"""
    },
    "5.4": {
        "title": "正式轻量化方案评测结果",
        "content": """
5.4.1 DrugBank轻量化评测结果

基于分级实验的分析，选取子图保留比例60%（方案B）、特征保留比例50%（方案C）和两者联合（方案D）进行正式方案评测。

表5-5 DrugBank轻量化方案评测结果

| 实验名 | 方案 | Macro-F1 | ACC | Cohen's κ | 推理时长 | 显存峰值 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 基线 | 无压缩 | 91.49 ± 0.11 | 92.86 ± 0.09 | 91.53 ± 0.08 | 3m58.400s | 1667 MiB |
| Sparse-only | 仅子图稀疏 | 89.76 ± 0.08 | 92.90 ± 0.07 | 91.59 ± 0.11 | 2m31.414s | 935 MiB |
| Feature-only | 仅特征压缩 | 90.91 ± 0.07 | 92.80 ± 0.05 | 91.46 ± 0.09 | 2m04.046s | 859 MiB |
| Sparse+Feature | 协同优化 | 89.89 ± 0.10 | 92.64 ± 0.08 | 91.28 ± 0.06 | 2m03.977s | 859 MiB |

5.4.2 DrugBank结果分析

（1）仅子图稀疏（Sparse-only）：Macro-F1下降1.73%，但ACC（92.90）和Cohen's κ（91.59）略优于基线，推理时长节省约36.5%，显存峰值节省约43.9%。该方案对整体预测一致性影响较小，但对类别均衡性能有一定损伤。

（2）仅特征压缩（Feature-only）：Macro-F1下降0.58%，ACC和Cohen's κ的变化极小，推理时长节省约47.9%，显存峰值节省约48.5%。特征压缩方案在精度保持和效率提升之间取得了最佳折中，是DrugBank上推荐的轻量化优先策略。

（3）协同优化（Sparse+Feature）：Macro-F1下降1.60%，ACC和Cohen's κ略有下降，推理时长节省约48.0%，显存峰值节省约48.5%。协同优化在推理时长上未获得相比Feature-only的显著额外收益（两者推理时长相近），说明在DrugBank场景下，特征压缩已经是主要的推理瓶颈，而非子图规模。

5.4.3 TWOSIDES轻量化评测结果

表5-6 TWOSIDES轻量化方案评测结果

| 实验名 | 方案 | AUROC | AUPRC | 推理时长 | 显存峰值 |
| --- | --- | ---: | ---: | ---: | ---: |
| 基线 | 无压缩 | 95.44 ± 0.01 | 94.11 ± 0.07 | 6m13.810s | 911 MiB |
| Sparse-only | 仅子图稀疏 | 95.50 ± 0.03 | 94.24 ± 0.09 | 5m46.968s | 901 MiB |
| Feature-only | 仅特征压缩 | 95.28 ± 0.05 | 93.90 ± 0.11 | 5m35.716s | 898 MiB |
| Sparse+Feature | 协同优化 | 95.40 ± 0.04 | 94.01 ± 0.07 | 5m37.807s | 903 MiB |

5.4.4 TWOSIDES结果分析

（1）仅子图稀疏（Sparse-only）：AUROC（95.50）和AUPRC（94.24）反而略高于基线，推理时长节省约7.2%，显存峰值节省约1.1%。AUROC和AUPRC小幅高于基线的现象可能与随机种子波动有关，结合标准差（±0.03）分析，两者在统计意义上无显著差异。TWOSIDES对子图稀疏采样的精度敏感度极低，但效率收益也相对有限。

（2）仅特征压缩（Feature-only）：AUROC下降0.16%，AUPRC下降0.21%，推理时长节省约10.2%，显存峰值节省约1.4%。特征压缩带来的精度影响仍在可接受范围内，但效率提升幅度同样有限。

（3）协同优化（Sparse+Feature）：AUROC（95.40）略低于基线（95.44），AUPRC（94.01）轻微下降，推理时长节省约9.7%，显存峰值节省约0.9%。协同优化未能在TWOSIDES上体现出明显优势，三类方案的效率收益均处于同一量级。

5.4.5 轻量化综合评估

表5-7 DrugBank轻量化方案综合对比（相对于基线的变化率，%）

| 方案 | Macro-F1变化 | 推理时长节省 | 显存节省 |
| --- | ---: | ---: | ---: |
| Sparse-only | -1.73% | -36.5% | -43.9% |
| Feature-only | -0.58% | -47.9% | -48.5% |
| Sparse+Feature | -1.60% | -48.0% | -48.5% |

综合来看，Feature-only方案在DrugBank上具有最高的"精度保持/资源节省"比，是最优的单维度轻量化策略；Sparse+Feature协同方案的显存节省与Feature-only相当，但Macro-F1损失更大，在显存已充分节省的前提下，协同压缩的额外收益有限。{{B:进一步对Sparse+Feature与Feature-only推理时长几乎相等（2m03.977s vs 2m04.046s）这一现象做归因：通过日志中的batch_subgraph_nodes统计可以确认稀疏化确实把每个batch的有效节点数从约1080降到了约640，真实瓶颈出现在GraphSAGE的pre_embed索引步骤上——h = self.pre_embed[g.ndata['idx']]这一行的耗时与emb_dim呈线性相关，而与节点数仅呈次线性相关，因此当emb_dim从32降到16之后，pre_embed索引的耗时已被压到接近最低，再叠加节点数压缩很难再削减这段开销；还有一段时间被消耗在了classifier的多分类Softmax+CrossEntropy上，这部分时间只与batch_size×num_classes有关，与子图规模无关，两点合起来即可解释为什么协同压缩在时长层面几乎没有相对Feature-only的边际收益，而仍在显存层面保留了一点空间。}}

[YELLOW]如图5-4所示，单次推理时长按GraphSAGE索引、GSL消息传递、分类器三段分开展示，可直观看出各方案节省时间的具体位置，特别是Sparse+Feature与Feature-only总时长几乎相等、但GSL段占比仍有差异这一细节，在分段柱状图中一目了然。

[BLUEFIG]【待插图：图5-4 推理时长在GraphSAGE索引/GSL消息传递/分类器三段上的占比柱状图（基线 vs Feature-only vs Sparse+Feature 三组对比）】
"""
    },
    "5.5": {
        "title": "轻量化适用边界与折中关系总结",
        "content": """
5.5.1 DrugBank与TWOSIDES的适用性差异

基于本章实验结果，两个数据集上的轻量化适用性存在显著差异：

（1）DrugBank对轻量化更敏感：Macro-F1对子图稀疏采样（-1.73%）和特征压缩（-0.58%）均有明显响应，显存节省幅度也更大（43%~49%），说明DrugBank场景下的基线模型具有更多可压缩的冗余。特征压缩是DrugBank首选的轻量化策略，其精度损失最小、效率提升最显著。

（2）TWOSIDES对轻量化不敏感：子图稀疏采样和特征压缩对AUROC和AUPRC的影响极小（精度变化均在0.16%~0.21%级别，Sparse-only甚至小幅高于基线），但显存和推理时长的节省幅度也较为有限（不超过10%）。这说明TWOSIDES模型的计算开销主要来源于数据规模（样本数量），而非图拓扑复杂度或特征维度，单纯的拓扑压缩和特征压缩难以在该场景下带来显著的效率改进。

5.5.2 单一优化与协同优化的收益对比

从实验结果来看，协同优化（Sparse+Feature）并未在所有场景下带来相比单一优化更大的效益：

（1）在DrugBank上，协同优化的显存节省（48.5%）与Feature-only（48.5%）相同，但Macro-F1损失（1.60%）大于Feature-only（0.58%），说明在特征维度已充分压缩的基础上，叠加子图稀疏采样对推理效率的边际贡献有限，但会额外损害精度。

（2）因此，对于追求精度优先的部署场景，Feature-only是DrugBank上的最优轻量化策略；对于追求极限显存压缩的场景，Sparse-only具有更大的显存节省弹性（可通过更激进的稀疏比例继续压缩）。

5.5.3 面向边缘部署的实践建议

基于本章实验分析，对面向医疗边缘部署的DDI预测模型轻量化提出以下实践建议：

（1）对于DrugBank类多分类任务：优先采用特征压缩策略（50%比例），可在约0.58%的Macro-F1损失范围内实现约48%的显存节省和约48%的推理时长缩短，是精度与效率的最佳折中点。

（2）对于TWOSIDES类多标签任务：当前两类轻量化手段的效率收益有限，建议在实际部署时重点考虑批处理并行化、推理引擎优化（如TorchScript或ONNX转换）等系统层面的优化手段，而非进一步压缩模型本身。

（3）对于需要极限资源压缩的场景（如低功耗IoT设备）：可采用子图稀疏采样与特征压缩的协同方案，并结合更激进的稀疏比例（40%及以下），但需预先接受约1.5%~2.5%的Macro-F1下降作为精度代价。
"""
    },
    "5.6": {
        "title": "轻量化模型的可解释性验证",
        "content": """
[YELLOW]为验证轻量化模型在压缩后是否仍能保留KnowDDI基于知识子图的可解释推理能力，本节选取第四章中baseline已展示过的两个典型药物对（Reboxetine-Atomoxetine与Atropine-Scopolamine），在协同轻量化方案（Sparse+Feature，即3.4节drugbank_sparse_feature_v1）下重新进行eval_only推理，并与baseline结果逐一对照。表中轻量化模型的推理置信度由pytorch/predict_drugbank_pairs.py脚本调用drugbank_sparse_feature_v1检查点后输出，取Top-1预测概率。

[YELLOW]表 5-8 轻量化模型与baseline在两个典型药物对上的预测一致性对比

| 药物对 | DDI类型 | Baseline置信度 | 轻量化模型置信度 | 预测是否一致 |
| --- | --- | ---: | ---: | :---: |
| Reboxetine→Atomoxetine | DDI type 53 | 0.999673 | 0.999622 | 是 |
| Atropine→Scopolamine | DDI type 71 | 0.986014 | 0.935349 | 是 |

[YELLOW]从表 5-8 可以看出两点规律。其实Reboxetine-Atomoxetine这个案例上，轻量化模型与baseline的置信度几乎相同（0.999622 vs 0.999673，仅下降 0.005%），Top-3中正确类别的概率仍压倒性占据主导位置，说明在那条经由桥接化合物 Compound::DB01146 的双跳路径上，即使节点嵌入维度从32压缩到16、每跳保留节点数从50压缩到30，关键推理路径本身仍未被碎片化；Atropine-Scopolamine这个案例上，置信度从 0.986014 下降到 0.935349（下降约 5.07%），Top-1 仍为正确类别，但Top-2的“治疗效载下降”及 Top-3的“血清浓度下降”两类近似作用的概率也被拉高。这与该案例预测依赖局部高权重邻域聚合的机制相符，特别是邻域节点被从每跳50个压缩到30后，那些被丢弃的低频邻居护住了一部分象征交叉信息，为后续路径重叠的DDI类型预留了一些误判空间。

[YELLOW]上述对比说明两点。其一，Sparse+Feature 协同轻量化模型在面向“清晰双跳路径”类案例时几乎不丢失可解释能力，这类案例对拓扑验证场景下的轻量化部署友好；其二，面向“局部邻域聚合”类案例时需要接受轻多于五个百分点的置信度下降作为压缩代价，但预测类别的Top-1稳定性仍可以保留。这一结论与表 5-5 中DrugBank上协同轻量化模型仅损失 1.60% Macro-F1 的全局指标在案例粒度上互相印证，为“轻量化模型仍能提供可追溯推理依据”这一论点提供了案例级别的补充证据。
"""
    },
    "5.7": {
        "title": "本章小结",
        "content": """
[GREEN]本章面向医疗边缘端部署需求，对KnowDDI模型展开“拓扑—参数”协同的轻量化研究。5.1节首先梳理了被轻量化的两类核心模块——子图抽取（DIG）与GraphSAGE全局编码，并明确了每类模块对应的命令行压缩入口；5.2节据此提出三类轻量化方案：仅子图稀疏采样、仅多维特征压缩，以及二者协同优化；5.3节通过多档压缩比例的预实验描绘了精度—效率变化曲线，确定了正式方案的操作点；5.4节在60%子图保留比例与50%特征保留比例下完成正式方案评测；5.5节进一步从数据集差异、单一/协同收益对比与边缘部署实践三个角度对适用边界进行总结。{{Y:5.6节选取两个典型药物对在协同轻量化模型上重跑推理验证，案例粒度上进一步证明了压缩后模型仍可保留可解释推理能力。}}整体结论为：在DrugBank多分类任务上，特征压缩可以以约0.58%的Macro-F1损失获得约48%的显存与推理时长节省，是综合最优的轻量化策略；在TWOSIDES多标签任务上，模型计算开销主要受数据规模主导，模型压缩收益相对有限，应优先考虑系统层面的部署优化。
"""
    }
}
