针对图神经网络特有的计算结构，拓扑压缩通过减少图传播过程中涉及到的节点数和边数来降低计算开销。具体来说，一方面可以在子图提取阶段，通过控制每跳保留的最大节点数（max_nodes_per_hop）和最大候选边数（max_links），从输入规模上降低图传播的计算复杂度，这也是本文采用的另一核心轻量化手段，其中max_nodes_per_hop作用于DIG子图BFS扩展阶段，max_links则作用于GSL阶段的批内候选完全图，两者协同决定了拓扑维度的整体复杂度；另一方面在消息传递过程中，只采样固定数量的邻居节点参与聚合，从而避免了处理高度节点所带来的计算瓶颈。

本章围绕这项研究所涉及到的基础理论与关键技术展开梳理，一是介绍了知识图谱与生物医学知识图谱的基本概念和典型实例，二是说明了图神经网络的核心原理以及GraphSAGE/GAT等代表性模型，三是讨论了面向小样本DDI预测的图结构学习与自适应子图抽取思路，四是梳理了在边缘部署需求驱动下的模型轻量化方法体系。这些内容为后续章节中针对KnowDDI模型的图结构学习机制解耦研究，以及面向资源受限场景的“拓扑—参数”协同轻量化设计打下了理论与技术基础。

本节围绕本章消融研究所依托的KnowDDI模型及其复现基线展开介绍，重点说明与图结构学习模块（GSL）相关的结构和参数，以便后续章节能够聚焦到“去噪”与“补全”这两类操作的解耦分析上。

GSL模块（pytorch/model/gsl_model.py）会在批内构造出候选的完全连接图，对每一条候选边都拼接源节点表示、目标节点表示以及关系嵌入，并输入到EdgeGateNetwork中得到综合门控得分、去噪得分（denoise_head）和补全得分（completion_head）。模型经过edge softmax归一化处理之后，再施加阈值化或Top-K稀疏化，最终就能在优化后的图结构上完成多轮的消息传递。

在KnowDDI当中，去噪和补全这两类操作被封装到同一个EdgeGateNetwork内进行联合训练，因此无法直接得到两者各自对预测性能所贡献的边际效果。

在背景知识图谱方面，本文使用了Hetionet v1.0作为BKG_file，里面共包含约47000个节点和约2250000条边，能够覆盖化合物、基因、疾病、通路等多种实体类型以及它们之间的相互关系。实验是在以下的硬件与软件环境中完成的：GPU采用NVIDIA GeForce RTX 4090（24GB显存），CPU为Intel Core i9系列，内存32GB DDR5；操作系统使用Ubuntu 22.04 LTS，Python 3.12，PyTorch 2.3.0+cu121，DGL 2.0+，CUDA 12.1，scikit-learn 1.3.0。

具体来说，基线模型在DrugBank上的推理总时长约为3m58s，GPU显存的峰值达到1667 MiB；在TWOSIDES上推理总时长约为6m13s，GPU显存峰值则为911 MiB。在医疗边缘端的场景下（比如便携式医疗终端、院内实时警报系统），这样的开销仍然难以满足低延迟、低功耗的部署需求。本章基于“拓扑-参数”协同压缩的思路，提出并评估了以下三类轻量化方案：方案A是只做子图稀疏采样（Sparse-only），方案B是只做多维特征压缩（Feature-only），方案C则是协同优化（Sparse + Feature）。

子图稀疏采样通过控制子图提取阶段的规模参数，从输入层面减少图传播的拓扑复杂度。核心调控参数有两个：一是max_nodes_per_hop，即每跳最大保留的节点数，通过降低这个参数，能够直接减小局部子图的节点规模，降低图传播的计算量；二是max_links，即批内最大的候选边数，通过降低这个参数，能够减少图结构学习模块中候选边的数量，降低边权重学习的计算开销。本章采用相对于基线的“子图保留比例”（60%）作为稀疏采样强度的统一描述，对应把max_nodes_per_hop从原始值按比例调减。具体到命令行的实施上，方案A（Sparse-only，60%保留）的关键改动是把--max_nodes_per_hop从基线的50降到了30、--max_links从100000降到了60000，其他维度的参数则保持原值不变。

多维特征压缩通过同比例减小各个关键维度的参数，能够在不改变模型整体结构的前提下降低参数量和中间激活的内存占用。核心调控参数有三个：一是emb_dim，即节点嵌入维度（基线值：32，压缩后：16）；二是gsl_rel_emb_dim，即GSL模块关系嵌入维度（基线值：8，压缩后：4）；三是MLP_hidden_dim，即边权重学习MLP隐层维度（基线值：64，压缩后：32）。

3.1节梳理了KnowDDI的整体框架以及GSL模块的内部结构，介绍了DrugBank、TWOSIDES这两个数据集和实验环境，并按照原论文协议完成了基线的复现，与原文报告值的偏差都控制在了合理范围内，作为后续所有对比实验的统一基准。3.2节针对GSL中“去噪”和“补全”这两类机制设计了系统的消融方案，通过新增--gsl_mode等入口参数把两类操作单独拎出来，构造出baseline、仅去噪、仅补全、去噪+补全这四组受控变体，并锁定了数据预处理、模型容量以及训练设置，以保证对比的纯粹性。3.3节面向医疗边缘部署的需求，从“拓扑—参数”这两条线出发，提出了只做子图稀疏采样、只做多维特征压缩以及二者协同这三类轻量化方案，并约定了60%子图保留比例和50%特征保留比例这两个统一描述参数。本章的方案设计为第五章的消融实验和轻量化评测提供了可以直接落地的实施依据。

然而，这一变体在ACC（92.66）和Cohen's κ（91.31）上都低于基线，说明去噪操作在提升类别均衡识别能力的同时，也可能对整体预测的一致性带来轻微的负面影响。这一现象表明，去噪机制更加专注于减少噪声边对少数类别表示所造成的干扰，有利于改善多分类任务当中的类间不平衡问题。

Completion-only模型在Macro-F1上（90.00）显著低于基线，不过在ACC（92.89）和Cohen's κ（91.57）上略微优于基线。这说明补全机制倾向于增强已有高频关系的结构表达，有利于整体预测一致性的提升，但是对类别均衡性能的提升效果比较有限，甚至有可能因为引入了错误的补全边而略微损害到少数类别的识别能力。

本章的消融实验对图结构学习中的去噪和补全机制进行了首次系统性的定量解耦，揭示出以下几条核心规律：一是去噪更加有利于提升类别均衡意义下的预测性能，补全则更有利于增强整体预测的一致性，两者在多分类任务（DrugBank）当中呈现出了可量化的差异化贡献模式；二是图结构学习机制的有效性具有数据集依赖性，在图结构相对稀疏、类别分布不均衡的场景（DrugBank）中，图结构优化能够带来有益的性能补偿，而在图结构已经相对充分、关联模式比较清晰的场景（TWOSIDES）中，引入额外的结构操作并不一定能带来增益。这一发现说明，去噪和补全并不是简单的叠加关系，而是会在不同的任务场景下呈现出不同的边际贡献和适用性边界，从而为KnowDDI图结构学习机制的功能解耦和性能分析提供了实证依据。

只做子图稀疏（Sparse-only）方案下，Macro-F1下降了1.73%，不过ACC（92.90）和Cohen's κ（91.59）略微优于基线，推理时长节省了约36.5%，显存峰值节省了约43.9%，这一方案对整体预测一致性的影响比较小，但是对类别均衡性能有一定的损伤。
只做特征压缩（Feature-only）方案下，Macro-F1下降了0.58%，ACC和Cohen's κ的变化极小，推理时长节省了约47.9%，显存峰值节省了约48.5%，特征压缩方案在精度保持和效率提升之间取得了最佳的折中，是DrugBank上推荐的轻量化优先策略。
协同优化（Sparse+Feature）方案下，Macro-F1下降了1.60%，ACC和Cohen's κ略有下降，推理时长节省了约48.0%，显存峰值节省了约48.5%；协同优化在推理时长上并没有获得相比Feature-only的显著额外收益（两者的推理时长相近），这说明在DrugBank场景当中，特征压缩已经是主要的推理瓶颈，而不是子图规模。

只做子图稀疏（Sparse-only）时，AUROC（95.50）和AUPRC（94.24）反而略微高于基线，推理时长节省了约7.2%，显存峰值节省了约1.1%；AUROC和AUPRC小幅高于基线的现象，有可能和随机种子的波动有关，结合标准差（±0.03）来分析，两者在统计意义上并没有显著差异，TWOSIDES对子图稀疏采样的精度敏感度极低，但是效率收益也相对有限。
只做特征压缩（Feature-only）时，AUROC下降了0.16%，AUPRC下降了0.21%，推理时长节省了约10.2%，显存峰值节省了约1.4%，特征压缩所带来的精度影响仍然处在可接受范围内，但是效率提升的幅度同样比较有限。
使用协同优化（Sparse+Feature）时，AUROC（95.40）略低于基线（95.44），AUPRC（94.01）有轻微下降，推理时长节省了约9.7%，显存峰值节省了约0.9%，协同优化并未能在TWOSIDES上体现出明显的优势，三类方案的效率收益都处在同一量级上。

从实验结果来看，协同优化（Sparse+Feature）并没有在所有场景下都能带来相比单一优化更大的效益。
在DrugBank上，协同优化的显存节省（48.5%）和Feature-only（48.5%）相同，但是Macro-F1的损失（1.60%）大于Feature-only（0.58%），说明在特征维度已经充分压缩的基础上，叠加子图稀疏采样对推理效率的边际贡献比较有限，反而会额外损害到精度。
因此，对于追求精度优先的部署场景，Feature-only是DrugBank上的最优轻量化策略；而对于追求极限显存压缩的场景，Sparse-only则具有更大的显存节省弹性（可以通过更加激进的稀疏比例继续压缩）。

并且基于本章的实验分析，在面向医疗边缘部署的DDI预测模型轻量化当中，可以归纳出以下三点建议：一是对于DrugBank这类多分类任务，优先采用特征压缩策略（50%比例），能够在约0.58%的Macro-F1损失范围内实现约48%的显存节省和约48%的推理时长缩短，是精度和效率的最佳折中点；二是对于TWOSIDES这类多标签任务，当前两类轻量化手段的效率收益比较有限，建议在实际部署时重点考虑批处理并行化、推理引擎优化（比如TorchScript或ONNX转换）等系统层面的优化手段，而不是进一步压缩模型本身；三是对于需要极限资源压缩的场景（比如低功耗IoT设备），可以采用子图稀疏采样和特征压缩的协同方案，并结合更激进的稀疏比例（40%及以下），不过需要预先接受约1.5%~2.5%的Macro-F1下降作为精度代价。

本章基于第3章的方案设计，对消融与轻量化这两条主线展开了系统的实证验证。消融实验（4.1～4.3节）显示，DrugBank上去噪机制取得了最高的Macro-F1（91.52），补全机制则在ACC和Cohen's κ上略微胜过基线，两者联合时整体一致性达到了最优；而TWOSIDES上由于补全激活比例偏低（约0.7%），各个变体都没有超过基线，这说明图结构学习对数据集的稀疏度和类别分布相当敏感。轻量化评测（4.4～4.6节）显示，DrugBank上特征压缩以约0.58%的Macro-F1代价换得了约48%的显存和推理时长节省，是性价比最高的单一方案，子图稀疏采样在显存压缩弹性上更强但是精度损失略大，两者协同的收益比较有限；TWOSIDES上由于开销主要受数据规模主导，因此模型层面的压缩收益并不显著。综合来看，去噪和补全在不同的任务场景中呈现出了差异化的贡献，而面向边缘部署则应当针对数据集特性来选择压缩策略——精度优先选特征压缩，显存极限选稀疏采样，TWOSIDES这类任务则更应该着眼于推理引擎和批处理等系统层面的优化。4.7节通过Reboxetine→Atomoxetine和Atropine→Scopolamine这两个典型药物对，对baseline和协同轻量化模型在案例粒度上的Top-1类别与置信度做了对比，两个案例的置信度损失都不超过0.05%，预测类别和Top-3排序保持稳定，使得协同轻量化方案在牺牲约1.6%精度的同时仍然保留了知识子图驱动的可解释推理能力，为本章面向资源受限医疗边缘部署的轻量化结论补充了案例粒度的实证支撑。

这项研究存在以下几点主要的局限性：（1）实验规模限制：本文的消融实验只在DrugBank和TWOSIDES这两个数据集上开展，研究结论的普适性有待于在更多DDI数据集（比如OGB-biokg、OGBL-ddi等）上进一步验证。（2）轻量化策略的局限性：本文采用的子图稀疏采样策略基于随机采样或简单的阈值控制，并没有引入基于节点重要性（比如PageRank、度中心性）的自适应采样策略，更智能的稀疏化方法有可能在相同压缩比例下取得更小的精度损失。（3）硬件评测环境的代表性：本文的推理时延和显存峰值测量是在标准的GPU服务器环境当中进行的，并没有在真实的医疗边缘设备（比如ARM Cortex处理器、嵌入式GPU）上进行实际的部署测试，边缘端的实际推理性能可能会与本文报告的结果存在差异。（4）可解释性分析的深度：本文主要通过定量的消融实验来分析图结构学习机制，并没有深入展开对典型药物对的案例研究和生物医学路径解析，可解释性分析的深度有待于加强。（5）补全机制的设计局限：当前补全机制在TWOSIDES上的激活比例极低（0.62%~0.79%），这说明补全策略对该数据集的适应性不足，未来需要针对不同的数据集特性设计出更具自适应性的补全阈值策略。

未来可以在当前基于固定阈值的图稀疏化基础上，引入基于图拓扑统计（比如节点度分布、聚类系数）的自适应阈值机制，使得去噪和补全操作能够根据不同药物对局部子图的特征来动态调整激活强度，提升图结构学习机制在不同数据分布下的普适性。

可以在本文轻量化架构的基础上，结合模型量化（INT8/FP16混合精度）、算子融合等推理引擎优化技术，把轻量化的KnowDDI模型实际部署到ARM Cortex-M、NVIDIA Jetson等医疗边缘硬件平台上，测量真实设备上的推理延迟和功耗，从而为面向医疗IoT场景的DDI实时预警系统提供实践基础。

消融实验揭示了图结构学习机制在DrugBank和TWOSIDES上的适用边界差异。未来可以探索基于元学习（Meta-Learning）或领域自适应（Domain Adaptation）的跨数据集迁移策略，使得模型能够自适应地在不同的DDI数据集特性下调整图结构学习的策略，提升模型的跨场景泛化能力。
